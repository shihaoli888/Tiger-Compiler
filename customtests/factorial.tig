/* 函数自我递归调用 */
let
    var ans:=1
    function factorial(n: int): int = 
    if n = 1
    then ( printInt(1); printStr(" "); 1)
    else (
        ans := n * factorial(n-1);
        printInt(ans);
        printStr(" ");
        ans
    )
in 
    factorial(8);
    printStr("\n")
end
