/* 嵌套函数, 静态链; if-then; if-then-else */

let
    var ans := 0
    function foo(num: int) =
    let
        function bar(a: int) =
            ans := a + 1
    in

    bar(1)
    /*
            for i := 1 to num do
            (
                bar(i)
            )
    */
    end
in
    foo(2)
end